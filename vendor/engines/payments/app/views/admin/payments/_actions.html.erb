<ul>
  <% if Admin::PaymentsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_payments_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_payment_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::PaymentsController.sortable? and Payment.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Payments"),
                 admin_payments_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Payments"),
                 admin_payments_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>