<% content_for :body_content_left do %>
  <ul id="payments">
    <% @payments.each do |payment| %>
      <li>
        <%= link_to payment.status, payment_path(payment) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
